CREATE TABLE `autores` (
    `autorId` INT(11) AUTO_INCREMENT PRIMARY KEY,
    `nombreAutor` VARCHAR(100) NOT NULL
)

CREATE TABLE `editoriales` (
    `editorialId` INT(11) AUTO_INCREMENT PRIMARY KEY,
    `nombreEditorial` VARCHAR(100) NOT NULL
)
